# v1.1 検証記録

## 軽量化の比較結果
1920×1080、丸ブラシ256px、160回描画。同一モザイク元データを使用。
- v1.0.1: Canvas読み出し480回・書き込み480回、約146ms。
- v1.0.2: 読み出し8回・書き込み84回、約26ms。
- 全ピクセルのRGBA値が一致。
- 時間はNode.jsの模擬Canvas上での1回の測定値で、ブラウザの速度改善率を保証するものではありません。
- キャッシュした複数フレームの描画が個別描画と同じ結果になること、変更のないフレームでは画像を再転送しないことを回帰テストで確認。

## 共有機能の確認
- share.jsの制御テスト: 準備中に共有を自動開始しないこと、クリックハンドラー内で同期的に共有APIを呼び出すことを確認。
- ファイル共有非対応時のプレビュー、キャンセル後の再試行、画像生成中に閉じた場合の破棄、生成エラー、Object URLの解放を確認。
- テストはDOMとnavigatorを模したNode.js環境。iOSの共有シート・長押しメニュー・写真への保存は未検証です。
- 再実行: `node tests/share.test.cjs`

## 確認済み
- app.js / raster.jsのNode.js構文チェック。
- 実際のピクセル置換関数をNode.jsで実行し、40個の重なった丸ブラシの全被覆72,492ピクセルが期待するモザイクRGBA値と一致することを確認。非選択ピクセルは不変。
- 同じブラシを再度重ねてもピクセル値が変わらないことを確認。
- 操作列の再実行が同じ結果になることを確認（履歴再描画で利用する処理）。
- 256pxタイルの境界をまたぐ処理、画像端、全面塗りつぶし、透明ピクセルへの置換を確認。
- HTMLのID重複・JavaScript参照先・ローカルファイル参照を確認。
- ZIP整合性を確認。

## 再現テスト
Node.jsがある場合、展開フォルダーで以下を実行できます。外部パッケージ不要です。

```
node tests/raster.test.cjs
```

## 未確認
実ブラウザでの表示・操作・PNG/JPEG保存、Windows実機、GitHub Pagesでの公開は未検証です。ピクセル処理のテストはCanvas入出力を模したメモリ上のコンテキストで実行しており、ブラウザ描画の確認ではありません。

## 公開前の確認
元画像を開き、丸ブラシを何度も重ねてなぞる。100%・拡大表示で線が残らないことを確認する。四角ブラシ・範囲選択・塗りつぶしも試し、元に戻す／やり直しの結果とPNG/JPEG保存結果を確認する。

## iPhone公開前の確認
SafariでHTTPS公開URLを開き、PNGとJPEGそれぞれで「写真に保存・共有」→「共有画面を開く」→「画像を保存」を試す。写真アプリで加工・寸法を確認する。キャンセル後の再試行、プレビュー長押し、既存のファイルダウンロードも確認する。
