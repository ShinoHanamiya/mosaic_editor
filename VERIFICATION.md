# v1.0.1 検証記録

## 確認済み
- app.js / raster.jsのNode.js構文チェック。
- 実際のピクセル置換関数をNode.jsで実行し、40個の重なった丸ブラシの全被覆72,492ピクセルが期待するモザイクRGBA値と一致することを確認。非選択ピクセルは不変。
- 同じブラシを再度重ねてもピクセル値が変わらないことを確認。
- 操作列の再実行が同じ結果になることを確認（履歴再描画で利用する処理）。
- 256pxタイルの境界をまたぐ処理、画像端、全面塗りつぶし、透明ピクセルへの置換を確認。
- HTMLのID重複・JavaScript参照先・ローカルファイル参照を確認。
- ZIP整合性を確認。

## 再現テスト
Node.jsがある場合、展開フォルダーで以下を実行できます。外部パッケージ不要です。

```
node tests/raster.test.cjs
```

## 未確認
実ブラウザでの表示・操作・PNG/JPEG保存、Windows実機、GitHub Pagesでの公開は未検証です。ピクセル処理のテストはCanvas入出力を模したメモリ上のコンテキストで実行しており、ブラウザ描画の確認ではありません。

## 公開前の確認
元画像を開き、丸ブラシを何度も重ねてなぞる。100%・拡大表示で線が残らないことを確認する。四角ブラシ・範囲選択・塗りつぶしも試し、元に戻す／やり直しの結果とPNG/JPEG保存結果を確認する。
